# 相比 csexton/debugger-action@master 安全一点，只能 github 上有密钥的机器访问

name: test_ssh_by_tmate

on:
  - workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
